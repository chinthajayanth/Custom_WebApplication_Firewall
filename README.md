# Custom_WebApplication_Firewall
This project is a functional WAF implementation that sits between the user and the web application. It analyzes raw HTTP/HTTPS traffic at the application layer to detect and block malicious activity.
